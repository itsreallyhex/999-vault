{
  "name": "999-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "999, as a desktop app. Vault and playlists only.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "fresh": "taskkill /F /IM vault999.exe >nul 2>&1 & tauri build && start \"\" \"src-tauri/target/release/vault999.exe\"",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
