{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to do. The Python scripts and Discord presence are driven from Rust, so the frontend needs no shell permission of its own.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
